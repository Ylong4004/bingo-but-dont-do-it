plugins {
    alias(libs.plugins.kotlin.plugin.serialization)
    id 'bingo.common'
}

base.archivesName.set("${project.archives_base_name}-integration-vanish")

dependencies {
    modCompileOnly(libs.vanish.get())
    compileOnly project(path: ":platform", configuration: "namedElements")
}