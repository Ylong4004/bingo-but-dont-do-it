plugins {
    id 'bingo.common'
}

base.archivesName.set("${project.archives_base_name}-integration-placeholders")

dependencies {
    modCompileOnly(libs.fabric.permissions.api.v2.get())
    compileOnly project(path: ":platform", configuration: "namedElements")
}